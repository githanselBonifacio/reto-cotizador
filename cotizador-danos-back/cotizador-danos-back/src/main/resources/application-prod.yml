spring:
  data:
    mongodb:
      uri: ${MONGODB_URI}

core-ohs:
  base-url: ${CORE_OHS_BASE_URL}
  api-key: ${CORE_OHS_API_KEY}

app:
  security:
    require-api-key: ${APP_SECURITY_REQUIRE_API_KEY:true}
    api-key: ${APP_API_KEY}
